# 프로젝트 가이드 (Claude Code용)

이 프로젝트의 모든 규칙은 @AGENTS.md 를 따른다. (단일 정본)

스킬은 `.claude/skills/` 에 있으며 `/이름`으로 바로 실행할 수 있다 (`/new-page`, `/figma-implement`, `/multi-review`, `/troubleshoot`).

개인용 로컬 규칙이 필요하면 `CLAUDE.local.md`에 적는다.
